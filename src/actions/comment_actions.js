import * as type from '../constants/actionTypes';
import * as api from '../constants/api';
import axios from 'axios';

//export const addComment = comment => dispatch => axios.post(`${api.ROOT}/posts/${comment.id}/comment`, comment)
//	.then(res => dispatch({ type: type.FETCH_POSTS, payload: res.data }));
